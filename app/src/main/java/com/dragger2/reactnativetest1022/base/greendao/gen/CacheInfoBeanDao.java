package com.dragger2.reactnativetest1022.base.greendao.gen;

import android.database.Cursor;
import android.database.sqlite.SQLiteStatement;

import com.dragger2.reactnativetest1022.base.greendao.bean.CacheInfoBean;

import org.greenrobot.greendao.AbstractDao;
import org.greenrobot.greendao.Property;
import org.greenrobot.greendao.database.Database;
import org.greenrobot.greendao.database.DatabaseStatement;
import org.greenrobot.greendao.internal.DaoConfig;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table "CACHE_INFO_BEAN".
*/
public class CacheInfoBeanDao extends AbstractDao<CacheInfoBean, Long> {

    public static final String TABLENAME = "CACHE_INFO_BEAN";

    /**
     * Properties of entity CacheInfoBean.<br/>
     * Can be used for QueryBuilder and for referencing column names.
     */
    public static class Properties {
        public final static Property _id = new Property(0, Long.class, "_id", true, "_id");
        public final static Property Key = new Property(1, String.class, "key", false, "KEY");
        public final static Property Version = new Property(2, String.class, "version", false, "VERSION");
        public final static Property Data = new Property(3, String.class, "data", false, "DATA");
        public final static Property Ext = new Property(4, String.class, "ext", false, "EXT");
        public final static Property TimeCreate = new Property(5, Long.class, "timeCreate", false, "TIME_CREATE");
        public final static Property LengthTime = new Property(6, Long.class, "lengthTime", false, "LENGTH_TIME");
    }


    public CacheInfoBeanDao(DaoConfig config) {
        super(config);
    }
    
    public CacheInfoBeanDao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
    }

    /** Creates the underlying database table. */
    public static void createTable(Database db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "\"CACHE_INFO_BEAN\" (" + //
                "\"_id\" INTEGER PRIMARY KEY AUTOINCREMENT ," + // 0: _id
                "\"KEY\" TEXT," + // 1: key
                "\"VERSION\" TEXT," + // 2: version
                "\"DATA\" TEXT," + // 3: data
                "\"EXT\" TEXT," + // 4: ext
                "\"TIME_CREATE\" INTEGER," + // 5: timeCreate
                "\"LENGTH_TIME\" INTEGER);"); // 6: lengthTime
    }

    /** Drops the underlying database table. */
    public static void dropTable(Database db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "\"CACHE_INFO_BEAN\"";
        db.execSQL(sql);
    }

    @Override
    protected final void bindValues(DatabaseStatement stmt, CacheInfoBean entity) {
        stmt.clearBindings();
 
        Long _id = entity.get_id();
        if (_id != null) {
            stmt.bindLong(1, _id);
        }
 
        String key = entity.getKey();
        if (key != null) {
            stmt.bindString(2, key);
        }
 
        String version = entity.getVersion();
        if (version != null) {
            stmt.bindString(3, version);
        }
 
        String data = entity.getData();
        if (data != null) {
            stmt.bindString(4, data);
        }
 
        String ext = entity.getExt();
        if (ext != null) {
            stmt.bindString(5, ext);
        }
 
        Long timeCreate = entity.getTimeCreate();
        if (timeCreate != null) {
            stmt.bindLong(6, timeCreate);
        }
 
        Long lengthTime = entity.getLengthTime();
        if (lengthTime != null) {
            stmt.bindLong(7, lengthTime);
        }
    }

    @Override
    protected final void bindValues(SQLiteStatement stmt, CacheInfoBean entity) {
        stmt.clearBindings();
 
        Long _id = entity.get_id();
        if (_id != null) {
            stmt.bindLong(1, _id);
        }
 
        String key = entity.getKey();
        if (key != null) {
            stmt.bindString(2, key);
        }
 
        String version = entity.getVersion();
        if (version != null) {
            stmt.bindString(3, version);
        }
 
        String data = entity.getData();
        if (data != null) {
            stmt.bindString(4, data);
        }
 
        String ext = entity.getExt();
        if (ext != null) {
            stmt.bindString(5, ext);
        }
 
        Long timeCreate = entity.getTimeCreate();
        if (timeCreate != null) {
            stmt.bindLong(6, timeCreate);
        }
 
        Long lengthTime = entity.getLengthTime();
        if (lengthTime != null) {
            stmt.bindLong(7, lengthTime);
        }
    }

    @Override
    public Long readKey(Cursor cursor, int offset) {
        return cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0);
    }    

    @Override
    public CacheInfoBean readEntity(Cursor cursor, int offset) {
        CacheInfoBean entity = new CacheInfoBean( //
            cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0), // _id
            cursor.isNull(offset + 1) ? null : cursor.getString(offset + 1), // key
            cursor.isNull(offset + 2) ? null : cursor.getString(offset + 2), // version
            cursor.isNull(offset + 3) ? null : cursor.getString(offset + 3), // data
            cursor.isNull(offset + 4) ? null : cursor.getString(offset + 4), // ext
            cursor.isNull(offset + 5) ? null : cursor.getLong(offset + 5), // timeCreate
            cursor.isNull(offset + 6) ? null : cursor.getLong(offset + 6) // lengthTime
        );
        return entity;
    }
     
    @Override
    public void readEntity(Cursor cursor, CacheInfoBean entity, int offset) {
        entity.set_id(cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0));
        entity.setKey(cursor.isNull(offset + 1) ? null : cursor.getString(offset + 1));
        entity.setVersion(cursor.isNull(offset + 2) ? null : cursor.getString(offset + 2));
        entity.setData(cursor.isNull(offset + 3) ? null : cursor.getString(offset + 3));
        entity.setExt(cursor.isNull(offset + 4) ? null : cursor.getString(offset + 4));
        entity.setTimeCreate(cursor.isNull(offset + 5) ? null : cursor.getLong(offset + 5));
        entity.setLengthTime(cursor.isNull(offset + 6) ? null : cursor.getLong(offset + 6));
     }
    
    @Override
    protected final Long updateKeyAfterInsert(CacheInfoBean entity, long rowId) {
        entity.set_id(rowId);
        return rowId;
    }
    
    @Override
    public Long getKey(CacheInfoBean entity) {
        if(entity != null) {
            return entity.get_id();
        } else {
            return null;
        }
    }

    @Override
    public boolean hasKey(CacheInfoBean entity) {
        return entity.get_id() != null;
    }

    @Override
    protected final boolean isEntityUpdateable() {
        return true;
    }
    
}
